<h1>Edit Sponsored Post</h1>

<div class="row">
  <div class="col-md-4">
    <h4>Guidelines for Sponsored Posts</h4>
    <ul>
      <li>Beachfront properties on Mars not allowed.</li>
      <li>Deer not welcome.</li>
      <li>Payment due upfront with hugs.</li>
    </ul>
  </div>
  <div class="col-md-8">
    <%= form_for [@sponsoredpost.topic, @sponsoredpost] do |f| %>
      <div class="form-group">
        <%= f.label :title %>
        <%= f.text_field :title, class: "form-control", placeholder: "Enter new sponsored post title" %>
      </div>
      <div class="form-group">
        <%= f.label :body %>
        <%= f.text_field :body, class: "form-control", placeholder: "Enter new sponsored post body" %>
      </div>
      <div class="form-group">
        <%= f.label :price %>
        <%= f.number_field :price, class: "form-control", placholder: "Enter new price in whole dollar amount without dollar sign" %>
      </div>
      <div class="form-group">
        <%= f.submit "Save", class: "btn btn-success" %>
      </div>
    <% end %>
  </div>
</div>
